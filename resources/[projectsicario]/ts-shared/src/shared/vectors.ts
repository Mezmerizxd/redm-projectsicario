export const Vector2 = (x: number, y: number) => {
  return({
    x,
    y
  })
}
export const Vector3 = (x: number, y: number, z: number) => {
  return({
    x,
    y,
    z
  })
}
export const Vector4 = (x: number, y: number, z: number, h: number) => {
 return({
   x,
   y,
   z,
   h
 }) 
}