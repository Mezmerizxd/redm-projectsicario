// Modules
import React from 'react';
import ReactDOM from 'react-dom';

import Spawn from "./nui/Spawn";

ReactDOM.render(
  <React.StrictMode>
    <Spawn />
  </React.StrictMode>,
  document.getElementById('root')
);