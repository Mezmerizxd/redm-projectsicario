// Modules
import React from 'react';
import ReactDOM from 'react-dom';

import AdminMenu from "./nui";

ReactDOM.render(
  <React.StrictMode>
    <AdminMenu />
  </React.StrictMode>,
  document.getElementById('root')
);